# Database Configuration
DATABASE_URL=
postgresql://${{PGUSER}}:${{POSTGRES_PASSWORD}}@${{RAILWAY_PRIVATE_DOMAIN}}:5432/${{PGDATABASE}}

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Optional: JWT Secret (for future auth features)
JWT_SECRET=your-secret-key-here-change-in-production

api_live_I5GeLO63d05nUdaqsV70dhnjkmx6seZPHeZO4zDD

“Improve this logo so it displays crisply and readably in the app. Keep the original concept: a simple humanoid figure inside a circular border with a heart at the center and three ‘signal’ lines near the head. Maintain the warm orange color scheme, but enhance contrast, smooth out the shapes, and sharpen the edges so it stays clear at small sizes. Create a clean, modern, scalable version with balanced spacing, even stroke widths, and no visual noise. Produce both a high-resolution raster version and a vector version with transparent background.